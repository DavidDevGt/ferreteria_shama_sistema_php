name: Deploy website on push
on:
  push:
    branches:
      - main  # Asegúrate de que esta es la rama correcta

jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Get latest code
      uses: actions/checkout@v3  # Actualizado a v3
    
    - name: Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.4  # Actualizado a v4.3.4
      with:
        server: ftp://ftpupload.net/  # Tu servidor FTP
        username: if0_35291493  # Tu nombre de usuario FTP
        password: ${{ secrets.FTP_PASSWORD }}  # Tu contraseña FTP almacenada como un secreto
